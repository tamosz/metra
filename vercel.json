{
  "framework": "vite",
  "buildCommand": "cd web && npm ci && npm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    { "source": "/metra", "destination": "/index.html" },
    { "source": "/metra/(.*)", "destination": "/$1" }
  ]
}

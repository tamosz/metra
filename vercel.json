{
  "framework": "vite",
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    { "source": "/metra/(.*)", "destination": "/metra/$1" },
    { "source": "/metra", "destination": "/metra/index.html" }
  ]
}
